export interface TopMenu {
  title: string;
  link?: string;
}

export interface ImageSlider {
  imgUrl: string;
  link: string;
  caption: string;
}

export interface Channel {
  id: number;
  icon: string;
  title: string;
  link: string;
}
